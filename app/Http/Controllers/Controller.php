<?php

namespace App\Http\Controllers;

/**
 * Base controller for the application. Kept intentionally light — the
 * Laravel slim skeleton no longer ships one, so front-end and admin
 * controllers extend this shared abstract.
 */
abstract class Controller
{
    //
}
